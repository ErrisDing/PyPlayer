{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "file_path=$(jq -r '.tool_response.filePath // .tool_input.file_path' /dev/stdin 2>/dev/null); if [ -n \"$file_path\" ] && [ -f \"$file_path\" ] && [[ \"$file_path\" == *.py ]]; then grep -nE '^[[:space:]]*[a-zA-Z_][a-zA-Z0-9_]*\s*$' \"$file_path\" 2>/dev/null | head -10 || true; fi",
            "timeout": 30,
            "statusMessage": "Checking variable initialization patterns"
          }
        ]
      }
    ]
  }
}
